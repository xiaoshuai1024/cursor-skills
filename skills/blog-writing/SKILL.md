---
name: blog-writing
description: 写博客文章或改文章时的统一入口。覆盖选题自检、定类型、搭骨架、起标题、写正文、配图、收尾、润色、验证到 hugo build 的全流程。本 skill 是工作流骨架,详细的风格规范、分类型规范、结构工具、选题同理心、润色 SEO 在 references/ 下的 5 个文件,按步骤按需加载。每次写新文章或大改现有文章前调用。
---

# 博客写作全流程

## 何时用本 skill

**写新文章**或**大改现有文章**(扩写、去 AI 味、改标题)时,先调用本 skill。它把写文章从「凭感觉」变成「按流程」,避免漏掉选题、结构、篇幅、配图、去味、润色、验证这些环节。

## 渐进式披露:本 skill 的文件结构

本 skill 采用标准 skill 结构。**SKILL.md(本文件)是工作流骨架,不重复细节**。详细规范在 `references/` 下,按步骤按需加载:

| 文件 | 何时读 | 管什么 |
|------|--------|--------|
| `references/topic-and-empathy.md` | **第 0 步 选题** | 选题六来源 + 三标准 + 主题上升法 + 读者同理心 |
| `references/post-types-guide.md` | **第 1 步 定类型** | 六类文章结构 + 篇幅 + 配图配额 + 掘金爆款化 + AI 辅助边界 |
| `references/structure-toolkit.md` | **第 3 步 搭骨架** | 金字塔原理 + MECE + 黄金圈 + 奥卡姆剃刀 |
| `references/style-guide.md` | **第 2/4/6 步 标题/正文/收尾** | 语气、排版、标题六模式、SEO 长尾、故事连贯 |
| `references/polishing-and-seo.md` | **第 7 步 润色** | 精简/准确/生动三目标 + SEO 落地 |

> 单一入口原则:写文章只调这一个 skill。它内部会指引你读哪个 reference,不用手动找。

## 核心工作流(从选题到发布,9 步)

### 第 0 步:选题自检(动笔前,决定成败的 60%)

写之前先过选题关。**读 `references/topic-and-empathy.md` 的「选题方法论」**,用三标准自检:

1. **分享欲**:不吐不快 vs 硬凑?(硬凑的放弃)
2. **稀缺性**:网上同类多吗?我的角度稀缺在哪?(新数据/新场景/新观点/更深一层)
3. **对他人价值**:读者读完带走什么,一句话说清?

**主题上升法**:具体项目经历 → 通用方法论(「我们项目的定时器」→「如何实现分布式定时器」),扩大读者面。上升要适度,别硬拔成空洞哲学。

选题通不过这三关,**换题或拆系列**,不要硬写。

### 第 1 步:定类型(决定结构和篇幅)

**读 `references/post-types-guide.md`**,先回答:**这篇属于哪一类?**(教程/深度/踩坑/最佳实践/观点/日常)。类型决定结构、篇幅、配图配额。一篇文章只允许**一个主导类型**。

判不准时,问「读者读完带走什么」:怎么做→教程;为什么→深度;别踩坑→踩坑;该这么做→最佳实践;一个观点→观点;一点共鸣→日常。

### 第 2 步:起标题(写作第一步,不是最后一步)

标题定调性,先把标题想清楚再写正文。**读 `references/style-guide.md` 标题章节**:

- **去掉方括号前缀**(【AI 研发实战】【SOP】)——降低点击欲
- **15-30 字,主副结构用冒号分隔**
- **套六种模式**:数字+痛点 / 揭秘 / 踩坑复盘 / 决策为什么 / 对比选型 / 成长路径
- **SEO 长尾思维**:标题 = 关键词 + 意图,副标题放读者会搜的词组合
- 写完用**自检三问**验:3 秒能看懂吗 / 会想点吗 / 内容能兑现吗

### 第 3 步:搭骨架(动笔前,决定文章好不好读)

**读 `references/structure-toolkit.md`**,用三步搭骨架法:

1. **定主张**(金字塔顶端):一句话写出这篇文章的核心判断/结论/目标。写不出 = 选题没想清,回第 0 步。
2. **MECE 拆分支**:支撑这个主张,分哪几个维度?用 MECE 五种分类法(二分/过程/要素/公式/矩阵)拆 3-5 个二级标题。
3. **奥卡姆剪枝**:层级 ≤3、同层 ≤7、奇数优先、能删则删。

**关系**:类型模板(`post-types-guide.md`)定文章有哪几段(容器),骨架工具定每段内部的逻辑。两者叠加,不是冲突。

### 第 4 步:写正文(边写边守三条硬约束)

**约束一:篇幅**。除日常随笔/观点型外,教程/深度/踩坑/最佳实践型**正文 ≥5000 中文字符**(不含 front matter / 代码块 / 图注)。不够就拆系列或补内容,不要硬凑。

**约束二:配图配额**(按篇幅,硬性,详见 `references/post-types-guide.md` 第八章):

| 篇幅 | 配图数 |
|------|--------|
| 5000-6500 字 | ≥ 3 张 |
| 6500-8500 字 | ≥ 4 张 |
| 8500-12000 字 | ≥ 5-6 张 |
| 12000+ | 无上限 |

基准 1 张 / 1800 字。**任何连续 1500 字纯文字都要补图**。

**约束三:去 AI 味**(本博客核心差异化,详见下文「去 AI 味实战手册」)。

**正文写作风格**(语气、段落、代码、句式)见 `references/style-guide.md` 第二、三章。

**代码例子偏好**:优先用**后端**例子(Java/Maven/Spring/SQL),构建命令用 `mvn package`/`gradle build`,测试用 JUnit 语境;除非文章本身就是讲前端,否则不放 Vue 组件、webpack 配置、`npm run dev` 这类前端例子。

### 第 5 步:配图(用 drawio skill)

架构图/部署图/数据流/时序图 → 调用 `drawio` skill,用 mxGraph XML,**禁止 mermaid**。概念图/对比图 → Excalidraw 手绘风。

工作流:写 `.drawio` 源文件到 `static/diagrams-src/<slug>.drawio` → 用 drawio CLI 导出 SVG 到 `static/svg/<slug>.svg` → 文章里 `<img src="/svg/<slug>.svg" alt="描述性文字">`。

**公众号同步必须转 PNG**:公众号对 SVG 支持极差,`<img>` 引用 SVG 显示不出来。用 wechatsync 同步到公众号时,先把 SVG 转成 PNG:`rsvg-convert -w 1600 static/svg/<slug>.svg -o /tmp/<slug>.png`,再把文章里的 SVG 路径换成 PNG 本地路径——wechatsync 会自动把本地 PNG 上传到公众号图床。掘金/知乎是 Web 平台,SVG 正常显示,不用转。

**去 AI 味配色硬规则**:白底为主、最多 1 个主色(`#2563eb` 蓝 或 `#0f766e` 青绿)+ 深灰文字 `#1e293b`、字号 14/12 两级、箭头统一 `edgeStyle=orthogonalEdgeStyle`、标签说人话不要对仗短语、坐标手画不要等距对齐。

### 第 6 步:收尾(结论式,反 AI 味最关键的一环)

- **禁止用「检查清单/落地清单/Checklist」收尾**——最典型的 AI 味
- 收尾必须是**结论**:回到核心判断,1-3 段陈述句,讲清「我最终的观点/本质是什么/读者带走什么」
- **不要复述要点**(那是 PPT 的活)
- 全文**最后一段不能是列表**,必须是连贯文字
- 同理禁止:「以上就是…的全部内容」「希望对你有帮助」「你学会了吗」

### 第 7 步:润色(正向,让文章好读)

**读 `references/polishing-and-seo.md`**,过润色三目标:

- **精简**(Omit Needless Words):删冗余修饰、删口头禅、被动改主动、长句拆短句
- **准确**:术语精确到版本、数字具体(别用「很多/大幅」)、避免含糊
- **生动**:动词>名词、具体>抽象、场景>概念、类比(谨慎用)

润色是**正向**(把句子写好),去 AI 味是**负向**(删套话),两个都要。同时顺手过一遍 **SEO 清单**(标题含关键词、slug 用英文词、front matter 写 description、首段含关键词、图片有 alt、内链用锚文本)。

### 第 8 步:验证(发布前的门禁)

跑这个脚本,一次性检查字数、配图、SVG 完整性:

```bash
python3 -c "
import re, os, sys
slug = '$SLUG'  # 传入 slug
path = f'content/posts/{slug}.md'
t = open(path).read()
parts = t.split('+++\n',2)
body = parts[2] if len(parts)>2 else t
body = re.sub(r'\`\`\`.*?\`\`\`', '', body, flags=re.DOTALL)
body = re.sub(r'<img[^>]*>', '', body)
cn = len(re.findall(r'[\u4e00-\u9fff]', body))
imgs = len(re.findall(r'src=\"/svg/', t))
refs = re.findall(r'src=\"/svg/([^\"]+)\"', t)
missing = [r for r in refs if not os.path.exists(f'static/svg/{r}')]
ok = cn>=5000 and imgs>=3 and not missing
print(f\"{'✅' if ok else '❌'} {slug}: {cn}字 / {imgs}图\" + (f' 缺失SVG:{missing}' if missing else ''))
"
```

然后 `hugo --gc --minify --quiet` 确认构建无报错。

### 第 9 步:发布前终检清单

- [ ] 选题过三关(分享欲/稀缺性/对他人价值),主题上升到通用方法论
- [ ] front matter 用 TOML `+++` 围栏,`description` 写了(含关键词,SEO)
- [ ] `title` 无方括号前缀,符合标题规范 + SEO
- [ ] **骨架合规**:有明确主张,段落 MECE,层级 ≤3、同层 ≤7(第 3 步)
- [ ] 正文 ≥5000 中文字符(非日常/观点型)
- [ ] 配图达标,SVG 文件存在,图片有描述性 alt
- [ ] 全站 AI 味复扫通过(见下文)
- [ ] 润色三目标过一遍(精简/准确/生动)
- [ ] 收尾是结论式散文,不是列表
- [ ] **读者验证**:找无背景者读一遍,或自检模拟跳步/术语未解释(第 0 步同理心)
- [ ] 文章间用 `{{< relref "posts/xxx.md" >}}` 互链,融入成长线
- [ ] 同步发掘金时,套 `post-types-guide.md` 第十章爆款化(标题钩子/开头改写/互动引导)
- [ ] `hugo build` 成功

### 第 10 步:生成图文版(小红书/抖音/视频号同步发布)

> **并行产出**:写博客文章时,同步制作图文版已是伴随动作,非可选。图文版**只发布到社交平台,不发布到博客**。

正文写完后,同时为小红书/抖音/视频号生成一份精简图文版。

**1. 创建图文版 markdown 源文件**

在 `image-text/<slug>/` 下创建 `article.md`（**独立目录，不进入 Hugo 管线**）:

```toml
+++
title = '12字以内的短标题'    # 小红书/视频号标题 ≤ 20 字，抖音 ≤ 20 字
tags = ['AI', '人工智能', 'AI编程', '程序员', 'AIGC', '科技']
+++

正文内容...
```

> `image-text/` 目录下的任何文件**与博客完全分离**，不会被 Hugo 构建，也不会进入 Pages 仓。

如果文章已有博客版且想复用部分正文，也可以用旧方式在 `content/posts/<slug>-xhs.md` 创建（加 `draft: true`）。

**2. 正文要求**

- **标题 ≤ 20 字**(各平台 limit: 抖音 20 / 小红书 20 / 视频号 50),语义完整,不截断
- **正文 ≤ 1000 字**(视频号 ≤ 1000, 抖音 ≤ 5000, 小红书 ≤ 1000),用 emoji 分段(🔥🧩⚡),每段 ≤ 3 行
- 末尾加 `#AI #人工智能 #AI编程` 等文字标签(平台热搜词)
- **禁止** markdown `**` 标记和 `<img>` 标签(会被 prepare 脚本清洗掉)

**3. 生成信息卡片**

卡片存到 `image-text/<slug>/cards/` 目录下，命名固定顺序 9 张:

```
image-text/<slug>/cards/
  revolution.png
  fde.png
  skill.png
  memory.png
  context.png
  mcp.png
  loop_ctrl.png
  sandbox.png
  future.png
```

调用 `/content-cards` skill 生成 1080×1440 卡片图。

**4. 发布命令**

```bash
# 图文准备(提取正文 + 元信息)
python -m scripts.xiaohongshu.prepare --slug <slug> --source image-text

# 发布(需蚁小二客户端在运行)
python scripts/yxer_publish.py --slug <slug> --source image-text --platforms "抖音,小红书,视频号" --music 轻音乐

# 或全平台一起发(含公众号 Playwright)
make publish-all slug=<slug>
```

**5. 添加背景音乐(BGM) 可选**

通过 `--music` 参数可为图文添加背景音乐(抖音/小红书/视频号均支持):

```bash
# 搜索可用音乐
python scripts/yxer_publish.py --slug <slug>-xhs --platforms 抖音 --music 轻音乐 --music-list

# 发布时带 BGM（搜索关键词自动选第一首）
python scripts/yxer_publish.py --slug <slug>-xhs --platforms "抖音,小红书,视频号" --music 轻音乐
```

**BGM 预设关键词**: `轻音乐` / `纯音乐` / `科技感` / `钢琴` / `电子`

> 平台对图文 slideshow 自动循环播放短音乐(无需显式设置 loop 字段)。建议选 60s+ 的长曲以避免切歌感。

**6. 检查清单**

- [ ] 标题语义完整,≤ 20 字
- [ ] 正文 ≤ 1000 字,无 markdown 标记
- [ ] 9 张卡片图在 `static/img/xhs-cards/` 目录
- [ ] 标签用平台热门词(AI/人工智能/程序员等)
- [ ] 蚁小二客户端在运行(本机发布需要)
- [ ] (可选) BGM 已测试试听,风格合适

---

## 去 AI 味实战手册(全站清理沉淀,最高价值)

这是全站清理 56 篇文章后沉淀的方法。**AI 味是本博客的头号敌人**,写作时和改稿时都要盯紧。

### 五类 AI 味(从最严重到最轻)

#### 1. 补丁式总结段(最严重,跨篇通用)

AI 最爱在文末贴一段「## 回到本质:...」重复前文。**特征**:章节标题含「回到本质」「本质是」「核心是」,内容是对正文的复述和升华。

**处理**:整段删除。有新信息的挑出来并入正文相应位置;没有就直接删。判不准时问「这段删了,读者会少知道什么吗」——答不上来就删。

#### 2. 「我的判断是」前缀(最高频)

句子去掉「我的判断是」照样成立,这是 AI 的口头禅。变体:「我的判断很直接」「先说我的判断」「关键判断是」。

**处理**:直接删前缀。

#### 3. 空洞强调词

「真正的」「恰恰」「正是」「本质上」「核心在于」「终极」「护城河」「分水岭」「定时炸弹」。删掉意思不变,但立刻不像 AI。

**处理**:「真正的X」→ 多数删「真正的」(保留「真正的需求/问题/瓶颈」);「恰恰相反」→ 删;「护城河/分水岭/定时炸弹」→ 换具体词(优势/标志/隐患);「终极」→ 删。

#### 4. 三连对仗(机械感最强)

「把 X 移到 Y,把 A 扩成 B,把 C 串成 D」——三连对仗太工整,像口号。还有「不仅...而且...」「无论是...都...」。

**处理**:拆短句或删减。三连改两连或直接陈述。

#### 5. 模板化开头

「在「前端 → 全栈 → AI 开发」这条线上,这篇落在...」——系列文章一字不差重复,是机械感来源。

**处理**:系列连接保留(融入成长线),但每篇句式要变化。

#### 6. 套话开场词(PPT 体标志,来自爆款改造 SOP)

这些词出现在开头/段首,基本就是「工作汇报体」:「综上所述」「值得注意的是」「值得一提的是」「不难发现」「毋庸置疑」「本质上,」(句中作过渡也算)「在...的今天」「随着...的发展」。

**处理**:整句删除或改写。这些词删掉,后面的内容直接接上,读起来更干净。

#### 7. PPT 名词滥用(大词堆砌,显虚)

这些是「PPT 高频词」,一出现文章质感就掉:「赋能」「闭环」「抓手」「落地」「链路」「体系化」「底层逻辑」「全生命周期」「降本增效」「技术底座」。

**处理**:
- 多数直接换具体词:「赋能业务」→「帮业务做成 X」;「降本增效」→「从 3 天降到半天」;「底层逻辑」→「原理」。
- **业务术语例外**:「SaaS 商业闭环」「数据闭环」这类,如果真在描述一个完整的业务/数据回路,是合理术语,保留。判不准问「这个词换成『流程/回路/链路』意思变吗」——不变就换。
- **反讽/引用例外**:「声称"赋能了业务"但拿不出数据」是反讽用法,保留。

#### 8. 对仗句式(机械感,来自 SOP)

「不只是 X,更是 Y」「既...又...」「一方面...另一方面...」——单个对仗偶尔可用,但连用或凑成「三连」就显机械。

**处理**:单个对仗可留;连续出现就拆成短句。三连对仗(第 4 类)必删。

### 心法:工作汇报体 → 真人分享体

上面八类 AI 味,本质都是「工作汇报体」。改稿的心法一句话:**把「工作汇报体」改成「真人分享体」**——改完读出来,要像一个真人在工位上跟你聊,而不是一份周报在念稿。

光删还不够,要**补进人味**(这和 `polishing-and-seo.md` 的正向润色一致):

- **第一人称 + 口语**:「我以为」「老实讲」「我之前也踩过」「这里绕了弯路」「说真的」。允许不完美和小情绪。
- **具体代抽象**:「提升效率」→「从 3 天降到半天」;「优化体验」→「首屏从 4 秒到 1 秒」。
- **长短句混用**:偶尔来个短句(「那只占一小块。」「根本扛不住。」),别每句都一样长。

### 去 AI 味扫描脚本(发布前/改稿后跑一遍)

扫全站或单篇,定位命中点。**注意:扫描结果会有误报,必须人工判断**(见下文「误报处理」):

```python
import re, sys

# 命中即提示人工判断的规则字典
PATTERNS = {
    # 补丁段(最严重,标题命中基本必删)
    "补丁段标题": r"^##\s*[^\n]*(回到本质|本质是|核心是)[^\n]*",
    # 前缀/套话
    "我的判断是": r"我的判断(是|很直接|，|,)",
    "值得注意/一提": r"值得(注意|一提)",
    "不难发现/毋庸置疑": r"(不难发现|毋庸置疑)",
    # 空洞强调词
    "核心在于": r"核心在于",
    "终极": r"终极(?!服)",
    "恰恰": r"恰恰",
    "护城河/分水岭/定时炸弹": r"(护城河|分水岭|定时炸弹)",
    # 真正的(排除有实质后缀的合理用法)
    "真正的(需人工判)": r"真正的(?!需求|问题|瓶颈|价值|难点|痛点|实力|区别|用武之地|意思|挑战|门槛|文件|python)",
    # PPT 名词(多数该换,业务术语/反讽需人工判)
    "PPT名词(需人工判)": r"(赋能|闭环|抓手|底层逻辑|全生命周期|降本增效|技术底座)",
    # 套话开场
    "套话开场": r"(综上所述|在.{2,15}的今天|随着.{2,15}的发展)",
    # 对仗(单个可留,三连必删)
    "对仗(单个可留)": r"(不只是.{2,20}更是|既.{2,15}又.{2,15}|一方面.{2,30}另一方面)",
    # AI 味收尾
    "AI味收尾": r"(以上就是|希望对你有帮助|你学会了吗)",
}
```

### 误报处理(扫描结果必须人工判断,不能无脑全删)

扫描会命中三类误报,**不能删**:

1. **front matter 里的 tags/keywords**:如 `tags = ['业务赋能']`——这是标签不是正文,不动。
2. **反讽/引用语境**:如「声称"赋能了业务"但拿不出数据」——这里"赋能"是被批评的对象,保留。
3. **业务术语合理用法**:如「SaaS 商业闭环」「数据闭环」——真在描述完整回路,是术语,保留。

**判断口诀**:**这个词换掉,意思变吗?** 不变就删/换;变了(变成反讽失效/术语不准)就留。拿不准的,留着——为了去味破坏内容是红线。

**批量删「回到本质」段**(Python 正则,从后往前删避免偏移):
```python
seg_pattern = re.compile(r"(^## [^\n]*回到本质[^\n]*\n)(.*?)(?=^## |\Z)", re.M | re.DOTALL)
```

**批量删/换高频词**(`re.subn` 统计替换数):
```python
rules = [
    (r"我的判断是[,，:：]\s*", ""),
    (r"核心判断[:：]\s*", ""),
    (r"恰恰相反", ""),
    (r"真正的(?!需求|问题|瓶颈|价值)", "实际"),
]
```

**批量验证残留**(目标 0 处或仅剩合理用法):
```python
patterns = {"回到本质":r"回到本质","我的判断是":r"我的判断是","终极":r"终极(?!服)",
            "恰恰":r"恰恰","护城河":r"护城河","分水岭":r"分水岭"}
```

### 去 AI 味的红线(不能为了去味破坏内容)

- **扫描结果必须人工判断**:见上文「误报处理」三类误报(tags / 反讽 / 业务术语),不能无脑全删
- **保留有对比/实质语义的「真正」**:「真正的需求」「真正的瓶颈」「压测真正的难点不在跑工具」里「真正」有实质作用,别全删
- **保留合理术语**:「PRD 的核心判断」是产品术语,「SaaS 商业闭环」是业务术语,不是套话
- 删完**复验字数**——去 AI 味会削字,别把达标文章削破线

---

## 踩过的坑(实战经验)

### drawio CLI 导出偶发失败

**现象**:`drawio --export` 对某些 `.drawio` 文件报 `Export failed`,但 XML 合法、xmllint 通过。排查过文件名/目录/symlink/xattr/进程残留,均非根因,疑似 CLI 对特定 cell 组合 + 文件元数据的兼容 bug。

**可靠 workaround**:源文件 `cp` 到 `/tmp/` 短路径 → 从 `/tmp/` 导出 SVG 到 `/tmp/` → `cp` 回 `static/svg/`。仍失败就简化图(减 cell 或拆子图)。

### 字数脚本只数纯中文字符

字数验证用 `[\u4e00-\u9fff]` 只数中文,英文术语和标点不计。这是**最严格**计数——纯中文都过 5000,加英文只会更多。别用「含英文」的宽松计数自欺欺人。

### 去 AI 味会削字,临界文章要复验

删「回到本质」段平均削 300-400 字。原本 5000+ 的文章删完可能破线。**每次去 AI 味后都要重跑字数脚本**,破线的补回(补有信息量的内容,不是补套话)。

---

## 标题六种模式速查(详见 style-guide.md)

| 模式 | 适用 | 例子 |
|------|------|------|
| 数字+痛点 | 教程/最佳实践 | `3 人两月交付 512 功能:30 条 AI 命令锁死测试纪律` |
| 揭秘 | 深度 | `低代码渲染引擎揭秘:schema 驱动到可编辑画布` |
| 踩坑复盘 | 踩坑 | `编译能过 ≠ 代码正确:一次 AI 静默删 270 行的事故复盘` |
| 决策为什么 | 最佳实践/观点 | `没用 K8s 也能自治:单机部署 + 夜间 AI Loop` |
| 对比选型 | 最佳实践 | `代开 vs 按租户打包:一份代码怎么变多租户小程序` |
| 成长路径 | 观点/日常 | `让研发长出产品脑:从需求到全自动开发` |

---

## 全站现状(写新文章前了解,避免重复造轮)

- 全站 56 篇,分 6 类,按「前端→全栈→AI 开发」成长线组织
- **历史欠账**:51 篇原本 <5000 字(写新文前查 `references/post-types-guide.md` 篇幅要求,别新增欠账)
- 已覆盖主题见各文章 slug,新文章尽量用 `relref` 互链,补全成长线
- 标题前缀问题已全站清除,新文章不要再加【XX】前缀
